{
  "schemaVersion": 1,
  "description": "Credential policy for manifests: placeholder-first and no committed real secrets.",
  "mode": "placeholder-first",
  "noCommittedRealCredentials": true,
  "allowPlaceholders": true,
  "sensitiveKeyPatterns": [
    "PASSWORD",
    "PASS",
    "SECRET",
    "TOKEN",
    "API_KEY",
    "KEY",
    "JWT",
    "AUTH"
  ],
  "disallowedWeakDefaults": [
    "password",
    "changeme",
    "casaos",
    "admin",
    "root",
    "your_password"
  ],
  "allowedPlaceholderValues": [
    "",
    "__SET_ME__",
    "__REQUIRED__",
    "REPLACE_ME",
    "${SECRET}",
    "${PASSWORD}",
    "${TOKEN}",
    "${API_KEY}"
  ],
  "exceptions": {
    "global": [],
    "byApp": {}
  },
  "notes": [
    "This policy blocks weak defaults but allows explicit placeholders for user-provided values.",
    "Use exceptions only when an app cannot function with placeholders during first-run setup."
  ]
}
